{
  "name": "accesslink",
  "version": "1.0.0",
  "description": "Find truly accessible travel options for wheelchair users",
  "type": "module",
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm run preview",
    "lint": "echo 'Linting...'",
    "verify": "node verify.mjs",
    "test": "cd app && npm run test || echo 'No tests'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@vercel/node": "^3.2.3",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "vitest": "^1.3.0"
  }
}
